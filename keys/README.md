# TLS keys

Place your AirDrop TLS certificate here (or let the tool auto-generate a self-signed pair on first run):

- `certificate.pem`
- `key.pem`
- `apple_root_ca.pem` (optional, for /Discover verification)

**Do not commit real private keys to Git.**
